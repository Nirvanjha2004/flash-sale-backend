{
  "name": "flash-sale-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "autocannon": "^8.0.0",
    "ioredis": "^5.3.2",
    "kafkajs": "^2.2.4"
  },
  "devDependencies": {
    "axios": "^1.6.0",
    "prisma": "^5.0.0"
  },
  "scripts": {
    "db:migrate": "npx prisma migrate dev --schema shared/database/schema.prisma"
  }
}
