{
  "name": "flash-sale-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "dependencies": {
    "kafkajs": "^2.2.4",
    "ioredis": "^5.3.2",
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "prisma": "^5.0.0",
    "axios": "^1.6.0"
  },
  "scripts": {
    "db:migrate": "npx prisma migrate dev --schema shared/database/schema.prisma"
  }
}
